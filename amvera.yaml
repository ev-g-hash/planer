meta:
  name: task-planner
  description: Планировщик задач с Telegram-ботом
  toolchain:
    name: python
    version: "3.12"
  environment:
    - DJANGO_SECRET_KEY
    - DJANGO_DEBUG
    - TELEGRAM_BOT_TOKEN
    - TELEGRAM_CHAT_ID

build:
  dockerfile: Dockerfile

run:
  ports:
    - "8080:8080"