meta:
  name: task-planner
  description: Планировщик задач с Telegram-ботом
  toolchain:
    name: python      ← было type, стало name
    version: "3.12"

build:
  dockerfile: Dockerfile

run:
  ports:
    - "8080:8080"